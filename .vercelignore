# `vercel deploy` uploads the working directory and does NOT honour .gitignore —
# only this file. Keep local secrets and build artifacts out of the upload;
# real env values belong in the Vercel project settings (Production/Preview).
.env
.env.local
.env.*.local
.next
node_modules
